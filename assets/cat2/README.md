# Cat 2 legacy placeholders

Dev **WebM placeholder** loops for early Cat 2 experiments.

**Production clips** used by the app are in [`../clips/cat2/`](../clips/cat2/).

Regenerate placeholders:

```bash
npm run generate:cat2-clips
```
